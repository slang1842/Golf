<%= render :partial => 'share/header_menu' %>
<br />

<div id="hole_tab">
  <%=render :partial => 'games/hole_tab', :locals =>{ :holes_filtered => @holes_filtered, :start_hole => @start_hole, :end_hole => @end_hole, :game_id => @game.id, :active_hole => 1, :form_id => @form_id}%>
</div>

<div id="hit_tab">
  <%=render :partial => 'games/hit_tab', :locals =>{ :game_id => @game.id, :active_hole => 1, :form_id => @form_id, :active_hit => 1}%>
</div>

<div id="hit_edit_tab">
  <%=render :partial => 'games/form_'+@form_id, :locals => { :active_hole => @active_hole, :active_hit => @active_hit, :game_id => @game.id }%>
</div>
