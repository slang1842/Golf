<% @holes_filtered.each do |f|%>
<%  if f.hole_number == @active_hole %>
    <b><%= f.hole_number %></b>
    <% else %>
    <%= f.hole_number %> 
    <% end %>
    <%end%>
    <%=active_hole%> 
    <% if active_hole != @start_hole%>
        <%= link_to 'Previous', hole_prev_path( active_hole, game_id, form_id, hit_type ), :remote => true %>
<% end %>
<% if active_hole != @end_hole%>
        <%= link_to 'Next', hole_next_path(active_hole, game_id, form_id, hit_type), :remote => true %>
<% end %>
    
