
 <div class="game_input_wrap">
    <div class="tabs_wrap">
     
      <div class="clear"></div>
    </div>
    
    <div class="clear"></div>
<div class="holes_wrap">
  <div class="heading_wrap">
  <p>Bedrīte:</p>
  </div>
<% holes_filtered.each do |f|%>
<% if f.hole_number.to_i < active_hole.to_i %>

<a href="#" OnClick="$(#hit_edit_tab.formid).val().value('<%=form_id%>'); $(#hit_edit_tab.nexthole).val().value('<%=f.hole_number%>');$(#hit_edit_tab.active_hit).val().value('hole');$(form).submit;" ><%=f.hole_number%></a>

<% elsif f.hole_number.to_i == active_hole.to_i %>
<a href="#" OnClick="$(#hit_edit_tab.formid).val().value('<%=form_id%>'); $(#hit_edit_tab.nexthole).val().value('<%=f.hole_number%>');$(#hit_edit_tab.active_hit).val().value('hole');$(form).submit;" ><%=f.hole_number%></a>
<% else %>
<a href="#" OnClick="$(#hit_edit_tab.formid).val().value('<%=form_id%>'); $(#hit_edit_tab.nexthole).val().value('<%=f.hole_number%>');$(#hit_edit_tab.active_hit).val().value('hole');$(form).submit;" ><%=f.hole_number%></a>
    <% end %>
  
    <% end %>
    
   </div> 
</div>
</div>
