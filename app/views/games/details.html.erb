<%= render :partial => 'share/header_menu', :locals => {:header_menu_active_class => "game_results"} %>
<br />

<div id="hole_tab">
  <%=render :partial => 'games/hole_tab', :locals => {:holes_filtered => @holes_filtered, :start_hole => @start_hole, :end_hole => @end_hole, :game_id => @game.id, :active_hole => @start_hole, :form_id => @form_id, :hits => 0, :puts => 0}%>
</div>

<div id="hit_tab">
  <%=render :partial => 'games/hit_tab', :locals => {:game_id => @game.id, :active_hole => @active_hole, :form_id => @form_id, :active_hit => @active_hit, :status_hole => @status_hole}%>
</div>
<div id="hit_edit_tab">
  <%=render :partial => 'games/hit_form_details', :locals => { :active_hole => @active_hole,  :game_id => @game.id, :hit_planned => @hit_planned, :active_hit => @active_hit, :hit_real => @hit_real, :pair_hit => @pair_hit, :status_hole => @status_hole}%>
</div>
