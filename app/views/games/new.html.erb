<% if current_user %>
  <%= render :partial => 'share/header_menu', :locals => {:header_menu_active_class => "game_results"} %>
  <% else %>
<% end %>


<%= render :partial => 'form_' + params[:form_id].to_s %>

<%#= link_to 'Back', games_path %>
