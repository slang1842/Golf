<%= form_for(@game_statistic) do |f| %>
  <% if @game_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_statistic.errors.count, "error") %> prohibited this game_statistic from being saved:</h2>

      <ul>
      <% @game_statistic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
