<%= render :partial => 'share/header_menu' %>
<br />
  <div id="holes_tab">
     <%=render :partial => 'games/hole_tab', :locals => {:active_hole => @active_hole}%>
   </div>
<br />
<div id= "hits_tab">
 <%=link_to 'Game Plan', plan_path(@game.id), :remote => :true%>
 <%=link_to 'Game Results', results_path(@game.id), :remote => :true %>
 <%=link_to 'Results in Details', details_path(@game.id), :remote => :true%>

</div>
<div id= "hole_number"><%=@active_hole%></div>
<div id= "hit_edit_tab">
  
</div>
