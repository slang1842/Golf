<h1>Listing users_sticks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users_sticks.each do |users_stick| %>
  <tr>
    <td><%= link_to 'Show', users_stick %></td>
    <td><%= link_to 'Edit', edit_users_stick_path(users_stick) %></td>
    <td><%= link_to 'Destroy', users_stick, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Users stick', new_users_stick_path %>
